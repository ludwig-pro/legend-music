import type { ViewStyle } from "react-native";

export const ShadowDropdown: ViewStyle = {
    shadowColor: "black",
    shadowRadius: 12,
    shadowOffset: { height: 1, width: 1 },
    shadowOpacity: 0.8,
};
